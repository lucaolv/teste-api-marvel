import React from 'react'

const SearchBar = () => {
  return (
    <header>
      {/* Você vai construir o SearchBar com o logo aqui */}
      <h1>SearchBar (Logo)</h1>
    </header>
  )
}
export default SearchBar